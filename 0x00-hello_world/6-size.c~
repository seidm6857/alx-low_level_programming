#include <stdio.h>

int main(void)
{
  char a;
  int b;
  long int c;
  long long int d;
  float f;

  printf("size of a char: %lu byte(s)\n", (unsinged long) size of (a));
  printf("size of an int: %lu byte(s)\n", (unsinged long) size of (b));
  printf("size of a long int: %lu byte(s)\n", (unsinged long) size of (c));
  printf("size of a long long int: %lu byte(s)\n", (unsinged long) size of (d));
  printf("size of a float: %lu byte(s)\n", (unsinged long) size of (f));
