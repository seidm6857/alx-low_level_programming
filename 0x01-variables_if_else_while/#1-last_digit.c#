#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* more headers goes there */

/* betty style doc for function main goes there */

int main(void)
  
{
  int n;
int last digit of n
  srand(time(0));
  n = rand() - RAND_MAX / 2;
  /* your code goes here */
  last digit of  n = i
    if(i<5) {
	     printf("last digit of n is i and is greater than 5\n");
                }
    else if(i=0)
      {      

       printf("last digit of n is 0 and is zero\n");
      }
    else {
      printf("last digit of n is i and is less than  6 and not zero\n");
}
    return (0);
  
}
